#!/usr/bin/python3
def print_sorted_dictionary(a_dictionary):
    a_dict = list(a_dictionary.keys())
    a_dict.sort()
    for num in list_ord:
        print("{}: {}".format(i, a_dictionary.get(num)))
